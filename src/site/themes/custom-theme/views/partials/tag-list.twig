{% if tags %}
    <div class="post-tags">
        <h2>Tagged with:</h2>
        <ul>
            {% for term in tags %}
                <li><a href="{{ term.link }}">{{ term.name }}</a></li>
            {% endfor %}
        </ul>
    </div>
{% endif %}
